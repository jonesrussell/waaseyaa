---
work_package_id: WP03
title: 'Documentation: convention page + security-defaults pointer'
dependencies:
- WP01
requirement_refs:
- FR-007
planning_base_branch: main
merge_target_branch: main
branch_strategy: Planning artifacts for this feature were generated on main. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into main unless the human explicitly redirects the landing branch.
base_branch: kitty/mission-inertia-file-upload-csrf-01KQZJQJ
base_commit: c4a2845155df2dc6305bede3323454ba1a88f22b
created_at: '2026-05-06T22:16:21.358868+00:00'
subtasks:
- T014
- T015
- T016
shell_pid: "228874"
agent: "claude:opus-4-7:reviewer:reviewer"
history:
- timestamp: '2026-05-06T21:45:49Z'
  event: created
  actor: claude-opus-4-7
  note: Initial WP prompt generated by /spec-kitty.tasks
authoritative_surface: docs/
execution_mode: code_change
mission_id: 01KQZJQJV8XMG9C1PF7TVMKKHE
mission_slug: inertia-file-upload-csrf-01KQZJQJ
owned_files:
- docs/conventions/csrf-token-cookie.md
- docs/specs/security-defaults.md
tags: []
---

# WP03 — Documentation: convention page + security-defaults pointer

## Objective

Document the CSRF cookie convention so consumer-app developers (Inertia, vanilla fetch, server-rendered Twig) know how to consume it. This closes FR-007 and SC-3.

Three audiences, one page. Plus a one-line pointer from the existing security-defaults page so it's discoverable.

## Context

- **Mission spec**: `kitty-specs/inertia-file-upload-csrf-01KQZJQJ/spec.md` (FR-007, SC-3).
- **Plan**: `kitty-specs/inertia-file-upload-csrf-01KQZJQJ/plan.md`.
- **Quickstart** (already audience-shaped): `kitty-specs/inertia-file-upload-csrf-01KQZJQJ/quickstart.md` §2–§4 has runnable examples for all three audiences. Translate those into the docs page.
- **Contract** (authoritative source for cookie attributes and accepted-source list): `kitty-specs/inertia-file-upload-csrf-01KQZJQJ/contracts/csrf-token-cookie.md`.

This WP can run in parallel with WP02 once WP01 has landed (the docs reflect the implementation).

## Branch Strategy

- Planning base: `main`
- Merge target: `main`
- Lane B. The execution worktree path is allocated by `finalize-tasks` and surfaced in `lanes.json`. Use `spec-kitty agent action implement WP03 --agent <name>` to enter the lane.

## Subtasks

### T014 — Create docs/conventions/csrf-token-cookie.md

**Purpose**: A standalone page that explains the convention, with three runnable code examples.

**File**: `docs/conventions/csrf-token-cookie.md`

**Required sections** (mirror this structure):

1. **Overview** (2–3 sentences): What the convention provides. Mention that Inertia consumers get it for free.
2. **For Inertia + Vue consumers** (zero-code path): Use the example from `quickstart.md` §2. Show the before/after if it helps.
3. **For vanilla `fetch` consumers**: Use the example from `quickstart.md` §3. Show the cookie-read helper.
4. **For server-rendered Twig consumers**: Use the example from `quickstart.md` §4. Note that this path is unchanged.
5. **Cookie attributes** (table): Copy the table from `contracts/csrf-token-cookie.md` §1. Include a footer note: "Authoritative source: `kitty-specs/inertia-file-upload-csrf-01KQZJQJ/contracts/csrf-token-cookie.md`".
6. **Accepted token sources** (table): Copy the request-side table from `contracts/csrf-token-cookie.md` §2.
7. **What this convention does NOT do**: One bullet list — no per-route exemptions, no rotation policy change, no session driver change.
8. **Troubleshooting**: Use the table from `quickstart.md` §7.

**Steps**:

1. Read `quickstart.md` §1–§4 and §7 for content.
2. Read `contracts/csrf-token-cookie.md` §1, §2 for the tables.
3. Write the docs page. Use second-person voice ("you can use this by...") consistent with other docs in the repo (check `docs/specs/security-defaults.md` for tone).
4. Cross-reference: link to `docs/specs/security-defaults.md` from the overview, link to the contract from the cookie-attributes section.

**Validation**:

- [ ] All three audiences have a complete runnable example (not pseudocode).
- [ ] The cookie-attributes table matches the contract exactly.
- [ ] The accepted-sources table matches the contract exactly.
- [ ] At least one cross-reference to `docs/specs/security-defaults.md`.

### T015 — Update docs/specs/security-defaults.md

**Purpose**: Add a pointer to the new convention page so it's discoverable from the existing security docs.

**File**: `docs/specs/security-defaults.md`

**Steps**:

1. Read the existing file to find the CSRF section.
2. Add a one-sentence reference: "For the JS-readable token convention used by Inertia file uploads, see [conventions/csrf-token-cookie.md](../conventions/csrf-token-cookie.md)."
3. Place it inline with the existing CSRF discussion, not as a stray section.

**Validation**:

- [ ] One-sentence reference added inline.
- [ ] Link path resolves (relative path correct).
- [ ] No structural changes to the rest of the file.

### T016 — Verify docs build / lint

**Purpose**: Ensure no docs build or link-check breaks.

**Steps**:

1. Check `Taskfile.yml` for any docs-related tasks (e.g., `task docs:lint`, `task docs:build`). If present, run them.
2. If no docs CI exists in `Taskfile.yml`, do a manual proofread of both pages for: broken links, typos, incomplete code blocks.
3. Verify the relative link from `docs/specs/security-defaults.md` to `docs/conventions/csrf-token-cookie.md` resolves (the Markdown viewer in your editor will tell you).

**Validation**:

- [ ] Any docs-related Taskfile target exits 0.
- [ ] Relative links resolve.
- [ ] Code blocks are syntactically valid.

## Definition of Done

- [ ] `docs/conventions/csrf-token-cookie.md` exists with all 8 sections above.
- [ ] All three audience examples are runnable (not pseudocode).
- [ ] Cookie-attributes and accepted-sources tables match the contract.
- [ ] `docs/specs/security-defaults.md` carries a one-line pointer to the new page.
- [ ] Docs Taskfile targets (if any) green.

## Risks

| Risk | Mitigation |
|---|---|
| Documentation drift from contract over time | Both tables are copied from the contract with a footer note pointing to the contract as the authoritative source. Future changes update the contract first; docs follow. |
| Examples that look right but don't run | Each example is copied from `quickstart.md`, which mirrors the contract. Reviewer is asked to spot-check at least one example end-to-end. |
| Wrong relative link path | T016 verifies. |

## Reviewer Guidance (Opus)

- Verify the three code examples are runnable (mentally walk through each).
- Verify the cookie-attributes table in the docs matches `contracts/csrf-token-cookie.md` §1 exactly.
- Verify the accepted-sources table matches `contracts/csrf-token-cookie.md` §2 exactly.
- Verify the page doesn't leak implementation details that would rot fast (no internal class names, no test paths, no SHA references).
- Verify the security-defaults pointer is inline with the existing CSRF discussion, not a stray new section.

## Activity Log

- 2026-05-06T22:16:22Z – claude:sonnet-4-6:implementer:implementer – shell_pid=223898 – Assigned agent via action command
- 2026-05-06T22:20:04Z – claude:sonnet-4-6:implementer:implementer – shell_pid=223898 – Convention docs landed; security-defaults.md updated with inline pointer
- 2026-05-06T22:20:27Z – claude:opus-4-7:reviewer:reviewer – shell_pid=228874 – Started review via action command
