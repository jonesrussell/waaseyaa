---
work_package_id: WP04
title: Release notes + alpha.172 prep
dependencies:
- WP02
- WP03
requirement_refs:
- FR-007
- FR-008
- NFR-001
- NFR-003
- NFR-004
- NFR-005
planning_base_branch: main
merge_target_branch: main
branch_strategy: Planning artifacts for this feature were generated on main. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into main unless the human explicitly redirects the landing branch.
subtasks:
- T013
- T014
- T015
agent: "claude"
shell_pid: "36150"
history:
- timestamp: '2026-05-04T23:04:19Z'
  event: drafted
  note: 'Generated by /spec-kitty.tasks for issue #1388 mission.'
authoritative_surface: CHANGELOG.md
execution_mode: code_change
mission_id: 01KQTK95TKYCKCFA1C07XE0CFM
mission_slug: alpha-172-fielddefinition-invariant-fix-01KQTK95
owned_files:
- CHANGELOG.md
tags: []
---

# WP04 — Release Notes + Alpha.172 Prep

## Objective

Add the `[0.1.0-alpha.172]` `CHANGELOG.md` entry, run every gate, and confirm PR traceability so the alpha.172 release can be cut after merge.

## Context

- **Spec**: [`spec.md`](../spec.md) — see FR-007, FR-008, NFR-001, NFR-005.
- **Plan**: [`plan.md`](../plan.md) — Phase 1 § "Release notes design", and "Gates".
- **Workflow rules**: `docs/specs/workflow.md` — versioning, PR traceability, `bin/check-milestones`.

## Branch Strategy

- **Planning base**: `main`
- **Merge target**: `main`
- Single-lane execution. Begins after WP02 and WP03 merge.
- Implementation command:
  ```bash
  spec-kitty agent action implement WP04 --agent <name>
  ```

## Subtasks

### T013 — Add `CHANGELOG.md` `[0.1.0-alpha.172]` entry

**File**: `CHANGELOG.md` (repo root).

**Steps**:

1. Open `CHANGELOG.md`. Confirm the topmost version entry is `## [0.1.0-alpha.171]`. If it is something else, **stop** — another release has been cut and the new version number must be re-derived.
2. Insert above the alpha.171 entry:

   ```markdown
   ## [0.1.0-alpha.172] - <YYYY-MM-DD>

   ### Fixed

   - **groups, taxonomy: core `description` (and taxonomy `weight`) FieldDefinition now declares `targetEntityTypeId`** — `GroupsServiceProvider` and `TaxonomyServiceProvider` constructed core FieldDefinitions without `targetEntityTypeId`, which the alpha.171 binding invariant correctly rejected. Kernel boot now registers `group_type` and `taxonomy_vocabulary` cleanly. Discovered while upgrading Minoo to alpha.171 (mission `upgrade-waaseyaa-alpha-171-01KQTDC2` WP03). (#1388)

   ### Notes

   - For consumers upgrading from alpha.165 → alpha.171 → alpha.172, three additional reconciliations may be required (already shipped in alpha.171; restated for posterity):
     - `EntityType` constructor named param renamed: `fieldDefinitions:` → `_fieldDefinitions:`.
     - `ServiceProvider::setKernelResolver()` removed; replaced by `setKernelServices(KernelServicesInterface)` plus `mergeChildProvider()`.
     - `Waaseyaa\Api\JsonResponseTrait` redesigned (single `jsonApiResponse()` returning `application/vnd.api+json`); previous `json()`/`jsonBody()` surface dropped.
   ```

3. Replace `<YYYY-MM-DD>` with the current UTC date on the day of merge.
4. Match formatting (heading depth, bullet style, bold-prefix convention) of the surrounding alpha.171 entry. If alpha.171 uses different headings (`### Fixed` vs `### Bug Fixes`, etc.), follow the existing convention.

**Validation**:
- `head -30 CHANGELOG.md` shows the new entry as the topmost version.
- Markdown lint (if the repo uses one) passes.

### T014 — Run all gates

**Purpose**: Confirm the alpha.172 build is releasable.

**Steps** (run from repo root, in order — stop on the first non-zero exit):

```bash
composer cs-fix         # auto-fix any style nits the implementer didn't catch
composer cs-check       # confirm clean
composer phpstan        # PHPStan level 5
vendor/bin/phpunit      # full PHPUnit suite (NFR-001)
bin/check-package-layers
bin/check-composer-policy
tools/drift-detector.sh
```

Each must exit 0. Capture totals (test count, assertion count, time) from the PHPUnit run for the WP completion notes.

If any gate fails:
1. Stop immediately.
2. Investigate the failure before any further edit.
3. If the failure is unrelated to this mission (i.e. a flaky test or a pre-existing failure on `main`), surface it to the user; do **not** silently work around it.
4. If the failure is related, treat it as a real regression introduced by WP02 or WP03 and fix it in the appropriate WP's lane.

### T015 — Final review pass + PR traceability

**Purpose**: Make sure the eventual PR description complies with the workflow contract in `docs/specs/workflow.md` (Rule 4 — PR traceability).

**Steps**:

1. Re-read this mission's spec, plan, and tasks files. Confirm every requirement (FR-001 through FR-008, NFR-001 through NFR-005, C-001 through C-005) has a corresponding artifact (test, patch, or doc edit) on the lane.
2. Draft (or extend) the PR description with:
   - `Closes #1388` (or `Refs #1388` per the repo convention).
   - A reference to the Minoo discovery mission: `Discovery: Minoo upgrade mission upgrade-waaseyaa-alpha-171-01KQTDC2 WP03 (lane commit 2552a87 on kitty/mission-upgrade-waaseyaa-alpha-171-01KQTDC2-lane-a)`.
   - Spec Kitty mission reference: `Mission: 01KQTK95 (alpha-172-fielddefinition-invariant-fix-01KQTK95)`.
   - A "Test plan" checklist that mirrors the `Definition of Done` items below.
3. Confirm `bin/check-milestones` (if applicable per the GitHub-issue workflow) is clean.

**Validation**: PR description, when opened, will satisfy `docs/specs/workflow.md` Rule 4.

## Definition of Done

- [ ] `CHANGELOG.md` has the `[0.1.0-alpha.172]` entry as the topmost version.
- [ ] `composer cs-check` exits 0.
- [ ] `composer phpstan` exits 0.
- [ ] `vendor/bin/phpunit` exits 0 with zero failures and zero errors.
- [ ] `bin/check-package-layers` exits 0.
- [ ] `bin/check-composer-policy` exits 0.
- [ ] `tools/drift-detector.sh` exits 0.
- [ ] PR description references `#1388`, the Minoo discovery mission, and Spec Kitty mission `01KQTK95`.
- [ ] All four WP01 tests still pass after every gate run.

## Risks & Mitigations

- **R-J**: A flaky test in `tests/Integration/` fails sporadically. *Mitigation*: re-run the suite once; if it still fails, investigate and either fix or escalate. Do not retry-loop the gate.
- **R-K**: PHPStan flags an unrelated pre-existing issue. *Mitigation*: capture the issue, surface it, but do not expand WP04 scope to fix unrelated lint.
- **R-L**: A reviewer requests a different alpha version number. *Mitigation*: confirm with the user; the version is derived mechanically from the previous alpha (`172 = 171 + 1`), but if a release-engineering convention overrides that, follow the convention.

## Reviewer Guidance

- Confirm the CHANGELOG entry is well-formed and matches the surrounding format.
- Confirm every gate command's exit code in the PR comments or run logs.
- Confirm the PR description satisfies workflow.md Rule 4.
- Reject merging if any gate is skipped, suppressed, or replaced with a partial run.

## Reference

- `CHANGELOG.md` — release-notes location.
- `docs/specs/workflow.md` — PR traceability rules.
- `bin/check-package-layers`, `bin/check-composer-policy`, `tools/drift-detector.sh` — gate scripts.
- WP02 prompt: [`WP02-patch-providers.md`](./WP02-patch-providers.md).
- WP03 prompt: [`WP03-sweep-and-spec-hygiene.md`](./WP03-sweep-and-spec-hygiene.md).

## Activity Log

- 2026-05-04T23:57:25Z – claude – shell_pid=36150 – Started implementation via action command
- 2026-05-05T00:07:32Z – claude – shell_pid=36150 – WP04 complete in commit 7f2b7ded9. CHANGELOG entry for [0.1.0-alpha.172] added with #1388 fix description and migration-aid Notes block. All static gates green: cs-check, phpstan (1143 files / 0 errors), check-package-layers, check-composer-policy, drift-detector. Full PHPUnit: 7206 tests / 17405 assertions / 2 failures. The 2 failures are pre-existing QueueIntegrationTest pollution on lane base b6856776a (verified by reproducing on the unmodified base — same 2 tests, same message); filed separately as issue #1389. Mission delta: +8 new tests, zero new failures. Ready for review and merge.
