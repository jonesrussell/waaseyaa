#!/usr/bin/env bash
# Monorepo dev shim — delegates to the canonical script in packages/cli/.
# In consumer projects, use vendor/bin/waaseyaa instead (Composer symlinks it).
exec "$(dirname "$0")/../packages/cli/bin/waaseyaa" "$@"
